{-# BUILTIN NATURAL ℕ #-}

_ : 3 ^ 4 ≡ 12
_ =
  begin
    3 ^ 4
  ≡⟨⟩
    3 * (3 ^ 3)
  ≡⟨⟩
    3 * (3 * (3 ^ 2))
  ≡⟨⟩
    3 * (3 * (3 * (3 ^ 1))
  ≡⟨⟩
    3 * (3 * (3 * (3 * (3 ^ 0))
  ≡⟨⟩
    3 * (3 * (3 * (3 * 0))
  ≡⟨⟩
    81
  ∎
